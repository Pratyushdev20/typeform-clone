import { LoginForm } from "../../components/auth/LoginForm";

export const metadata = {
  title: "Log in | Typeform",
  description: "Log in to your Typeform account to build and manage forms.",
};

export default function LoginPage() {
  return <LoginForm />;
}
