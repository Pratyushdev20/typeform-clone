import { SignupForm } from "../../components/auth/SignupForm";

export const metadata = {
  title: "Sign up | Typeform",
  description: "Create your free Typeform account to build smarter data flows.",
};

export default function SignupPage() {
  return <SignupForm />;
}
